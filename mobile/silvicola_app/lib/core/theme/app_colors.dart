import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF2E7D32); // Verde forestal
  static const Color primaryLight = Color(0xFF60AD5E);
  static const Color primaryDark = Color(0xFF005005);

  // Secondary Colors
  static const Color secondary = Color(0xFF8D6E63); // Marrón tierra
  static const Color secondaryLight = Color(0xFFBE9C91);
  static const Color secondaryDark = Color(0xFF5F4339);

  // Accent Colors
  static const Color accent = Color(0xFFFF6F00); // Naranja

  // Background Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // GPS Status Colors
  static const Color gpsActive = Color(0xFF4CAF50);
  static const Color gpsInactive = Color(0xFF9E9E9E);
  static const Color gpsSearching = Color(0xFFFF9800);

  // Sync Status Colors
  static const Color synced = Color(0xFF4CAF50);
  static const Color pending = Color(0xFFFF9800);
  static const Color failed = Color(0xFFF44336);

  // Other
  static const Color divider = Color(0xFFE0E0E0);
  static const Color shadow = Color(0x1F000000);
}
