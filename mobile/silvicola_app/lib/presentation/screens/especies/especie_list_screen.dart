import 'package:flutter/material.dart';

class EspecieListScreen extends StatelessWidget {
  const EspecieListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Especies')),
      body: const Center(child: Text('Lista de especies - TODO')),
    );
  }
}
