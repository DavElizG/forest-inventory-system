import 'package:flutter/material.dart';

class ParcelaListScreen extends StatelessWidget {
  const ParcelaListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Parcelas')),
      body: const Center(child: Text('Lista de parcelas - TODO')),
    );
  }
}
