import 'package:flutter/material.dart';

class ParcelaFormScreen extends StatelessWidget {
  const ParcelaFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nueva Parcela')),
      body: const Center(child: Text('Formulario de parcela - TODO')),
    );
  }
}
