import 'package:flutter/material.dart';

class ArbolFormScreen extends StatelessWidget {
  const ArbolFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Registrar Árbol'),
      ),
      body: const Center(
        child: Text('Formulario de árbol - TODO: Implementar'),
      ),
    );
  }
}
