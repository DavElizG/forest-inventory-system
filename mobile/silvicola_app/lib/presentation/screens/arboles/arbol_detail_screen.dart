import 'package:flutter/material.dart';

class ArbolDetailScreen extends StatelessWidget {
  const ArbolDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Detalle de Árbol'),
      ),
      body: const Center(
        child: Text('Detalle de árbol - TODO: Implementar'),
      ),
    );
  }
}
